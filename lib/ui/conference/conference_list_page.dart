import 'package:flutter/material.dart';

class ConferenceListPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("会议列表"),
      ),
      body: ListView(
        
      )
    );
  }
}
